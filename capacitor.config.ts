import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.pintob.app',
  appName: 'RegisterAPP',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
